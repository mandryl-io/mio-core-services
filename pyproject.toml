[project]
name = "mio-core-services"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ollama>=0.6.2",
    "pipecat-ai[kokoro,local,mlx-whisper,runner,silero,whisper]>=1.6.0",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mio_core_services"]
